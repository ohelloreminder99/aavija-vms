// This route is deprecated and no longer in use.
// This file is stubbed to prevent build errors.
export default function SuggestLocationLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
